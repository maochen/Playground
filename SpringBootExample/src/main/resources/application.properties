# This is a sample properties file for external override to use.
# By default, not needed if mongodb instance runs on the same server.

#spring.data.mongodb.host=localhost
#spring.data.mongodb.port=27017
#spring.data.mongodb.username=
#spring.data.mongodb.password=